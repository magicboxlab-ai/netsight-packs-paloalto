spec_version: "1.0"
engine: jmespath
operation: show_bgp_rib
input_format: xml
output_shape: dict
description: "Scaffolded from sample XML for paloalto/show_bgp_rib"

root: response.result.entry

fields:
  flags:
    path: flags
    type: str
  loc_rib:
    path: '"loc-rib"'
    type: str
