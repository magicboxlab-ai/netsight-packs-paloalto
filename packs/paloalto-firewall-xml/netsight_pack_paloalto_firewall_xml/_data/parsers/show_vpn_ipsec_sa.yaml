spec_version: "1.0"
engine: jmespath
operation: show_vpn_ipsec_sa
input_format: xml
output_shape: dict
description: "Scaffolded from sample XML for paloalto/show_vpn_ipsec_sa"

root: response.result

fields:
  ntun:
    path: ntun
    type: str
  entries:
    path: entries
    type: str
