spec_version: "1.0"
engine: jmespath
operation: show_bgp_peers
input_format: xml
output_shape: dict
description: "Scaffolded from sample XML for paloalto/show_bgp_peers"

root: response

fields:
  result:
    path: result
    type: str
