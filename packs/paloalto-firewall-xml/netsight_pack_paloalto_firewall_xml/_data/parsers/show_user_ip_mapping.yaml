spec_version: "1.0"
engine: jmespath
operation: show_user_ip_mapping
input_format: xml
output_shape: dict
description: "Scaffolded from sample XML for paloalto/show_user_ip_mapping"

root: response

fields:
  result:
    path: result
    type: str
