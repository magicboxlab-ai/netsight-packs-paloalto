spec_version: "1.0"
engine: jmespath
operation: show_session_all
input_format: xml
output_shape: dict
description: "Scaffolded from sample XML for paloalto/show_session_all"

root: response

fields:
  @status:
    path: @status
    type: str
  result:
    path: result
    type: str
